# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=13.0
python-multipart>=0.0.12

# Database
sqlalchemy[asyncio]>=2.0.35
aiosqlite>=0.20.0
alembic>=1.14.0
greenlet>=3.0.0

# HTTP client
httpx>=0.28.0

# Auth
python-jose[cryptography]>=3.3.0

# Config
pydantic-settings>=2.6.0

# Logging
structlog>=24.4.0

# File ops
aiofiles>=24.1.0
jinja2>=3.1.4

# Serialization
orjson>=3.10.0

# Redis (optional but included)
redis>=5.2.0

# Task queue (optional but included)
celery[redis]>=5.4.0

# Subtitle engine (optional, degrades gracefully if missing)
subliminal>=2.2.1

# Cloudflare bypass (optional, degrades gracefully if missing)
playwright>=1.49.0
